#ifndef MENU_H
#define MENU_H

void intro(char* Aux);
void MenuState(char* ReturnStates);
void MenuAlpha(char* Alpha);
void MenuFinish(char* States,char* FStates);
void MenuDelta(char* States, char* Alpha);

#endif
